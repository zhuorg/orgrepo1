name: Push 

on:
  issue_comment:
    types: [created, edited]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: octokit/request-action@v2.x
        id: get_PR_commits
        with:
          route: GET /repos/:repo/pulls/:pull_number/commits
          repo: ${{ github.repository }}
          pull_number: ${{ github.event.issue.number }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run:  echo $response
        env: 
          response:  ${{ tojson(steps.get_PR_commits.outputs.data) }}
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: 
          echo "$GITHUB_CONTEXT"
          echo ${{github.event.issue.number}}
#       
#     - uses: actions/checkout@v2
#       with:
#         repository: zhuorg/hello-github-actions
#         token: ${{secrets.PAT_FOR_PUSH}}
#     - name: Create local changes
#       run: |
#         cat > sales661.txt
#     - name: Commit files
#       run: |
#         git config --local user.email "action@github.com"
#         git config --local user.name "github-actions"
#         git add --all
#         git commit -m "Add changes" -a
#         git push https://$USERNAME:$REPO_KEY@github.com/zhuorg/hello-github-actions.git $GITHUB_REF
#       env:
#         REPO_KEY: ${{secrets.PAT_FOR_PUSH}}
#         USERNAME: github-actions
