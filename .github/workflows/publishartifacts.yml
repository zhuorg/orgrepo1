name: Upload Release
on: 
 push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v2
       - name: writeout token
         run: |
           echo "Token: ${TOKEN:0:4} ${TOKEN:4}"
         env: 
          TOKEN: ${{secrets.GITHUB_TOKEN}}

#        - run: sleep 50s
 
