on: push


jobs:
  version:
    runs-on: ubuntu-latest
    name: get version
    steps:
    - name: get version
      run:  |
        echo ${GITHUB_REF:11}
        echo "::set-env name=RELEASE_VERSION::${GITHUB_REF:11}"
    - run: echo $RELEASE_VERSION
