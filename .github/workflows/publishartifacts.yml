name: Upload Release
on: 
 release:
   type: [published]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v2 
       - run: echo $EVENT
         env: 
           EVENT: ${{ tojson(github.event) }} 
    
