on: push


jobs:
  version:
    runs-on: ubuntu-latest
    name: get version
    steps:
    - name: get version
      run:  |
        echo $RELEASE_VERSION
        $version = ${RELEASE_VERSION:11}
        echo $version
      env:
        RELEASE_VERSION: ${{ github.ref }}

